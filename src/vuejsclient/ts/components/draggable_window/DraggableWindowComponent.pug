.draggable_window
    vue-draggable-resizable.row.dragsize(
        :id='"dragsize_" + uid' 
        :resizable="false" 
        ref="dragsize" 
        :w="width" 
        :h="height" 
        :minh="minh" 
        :minw="minw" 
        :x="x" 
        :y="y" 
        :z="10000" 
        :drag-handle="'#dragsize_handle_' + uid" 
        :class="{'opened':opened, 'closed':!opened}")

        .col-sm-12
            .row.dragsize_handle(:id='"dragsize_handle_" + uid')
                .col-sm-12  
                    i.fa-duotone.fa-square-minus.fa-fw.pull-right(aria-hidden="true" v-show="opened" @click="opened = !opened")
                    i.fa-duotone.fa-square-plus.fa-fw.pull-right(aria-hidden="true" v-show="!opened" @click="opened = !opened")
                    i.fa-duotone.fa-up-down-left-right.fa-fw.pull-right(aria-hidden="true")

                    slot(name="icons")
            .row.dragsize_content
                .col-sm-12.no-padding
                    .slot_wrapper(ref="wrapper")
                        slot