.var_desc_registrations
    .row
        .col-md-9
            svg(width=1200 height=800)
        .col-md-3
            .row
                .col-md-12
                    button.btn.btn-sm.btn-primary.pull-right(@click="refreshDependenciesHeatmap")
                        i.fa.fa-map.fa-lg.btn_icon(aria-hidden="true")
                        | {{ label('var_desc.refreshDependenciesHeatmap') }}
            
            .row
                .col-md-12
                    hr

            .row(v-if='vardag_size')
                .col-md-12
                    h5 {{ label('var_desc_registrations.vardag_size') }}
                    .vardag_infos {{ vardag_size }}

                    h5 {{ label('var_desc_registrations.vardag_registered_prct') }}
                    .vardag_infos {{ vardag_registered_prct  +'%' }}

                    h5 {{ label('var_desc_registrations.vardag_registered_prct_by_var_id') }}
                    ul 
                        li(v-for='(vardag_registered_prct_by_var_id, var_id) in vardags_registered_prct_by_var_id') {{ var_id + ' : ' + vardag_registered_prct_by_var_id + '%' }}

            .row
                .col-md-12
                    button.btn.btn-sm.btn-secondary.pull-right(@click="clearDag")
                        i.fa.fa-trash.fa-lg.btn_icon(aria-hidden="true")
                        | {{ label('var_desc.clearDag') }}
            
            .row
                .col-md-12
                    hr

            .row
                .col-md-12(v-if='(!is_stepping)')
                    button.btn.btn-sm.btn-secondary.pull-right(@click="switch_stepper")
                        i.fa.fa-pause.fa-lg.btn_icon(aria-hidden="true")
                        | {{ label('var_desc.pause') + ' (' + getStepNumber + ')' }}
                .col-md-12(v-if='is_stepping && is_waiting')
                    button.btn.btn-sm.btn-secondary.pull-right(@click="continue_stepper")
                        i.fa.fa-play.fa-lg.btn_icon(aria-hidden="true")
                        | {{ label('var_desc.next') + ' (' + getStepNumber + ')' }}
                .col-md-12(v-if='is_stepping')
                    button.btn.btn-sm.btn-secondary.pull-right(@click="switch_stepper")
                        i.fa.fa-forward.fa-lg.btn_icon(aria-hidden="true")
                        | {{ label('var_desc.play') + ' (' + getStepNumber + ')' }}
            .row
                .col-md-12
                    var-desc(:var_param='descSelectedParam')
                    //- .hover_desc {{ hover_desc }}
            .row
                .col-md-12
                    textarea(rows=6, cols=30, id="graph_params", v-model="graph_params")
            .row
                .col-md-12
                    button.btn.btn-sm.btn-secondary.pull-right(@click="createGraph")
                        i.fa.fa-refresh.fa-lg.btn_icon(aria-hidden="true")
                        | {{ label('var_desc.create_graph') }}
